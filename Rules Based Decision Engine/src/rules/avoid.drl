package rules;
import models.*;
dialect  "mvel"

rule "avoid"
    when
        e1 : Entity (getValue("TRAESUB1.VALUE") < 50)
        e2 : Entity (getValue("TRAESUB2.VALUE") > 50)
    then
        System.out.println("Too Cold!");
end
