package rules;
import models.ObjectData;
dialect  "mvel"

rule "Too Cold"
    when
        d : ObjectData( d.getData() <= 50 )
    then
        System.out.println(d.getName() + " too cold; Move away!");
end



