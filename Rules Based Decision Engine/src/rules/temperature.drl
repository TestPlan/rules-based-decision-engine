package rules;
import models.*;

dialect  "mvel"

rule "temperature"
    salience 2
    when
       e1: Entity (getValue("TRAESUB1.VALUE") < 50)
    then
        System.out.println(e1 + "drl filepath works!\n");
end
